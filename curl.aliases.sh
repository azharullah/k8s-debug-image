# Inspiration: https://github.com/Bash-it/bash-it/blob/master/aliases/available/curl.aliases.bash

# follow redirects
alias cl='curl -L'
# allow insecure curl
alias ck='curl -L'
# follow redirects with insecure curls
alias clk='curl -L -k'